---
title: 双指针循环链表的数学内在
date: 2025-12-12 16:30:06
tags:
---

快慢指针是判断循环链表的常用方法之一。
快慢指针的原理是：快指针每次移动两步，慢指针每次移动一步，如果链表有环，那么快慢指针最终会相遇。

同时，可以依靠快慢指针找到环的入口节点。

- 当 `slow` 和 `fast` 在环中相遇时，记录下这个相遇点。
- 将 `slow` 指针重置到链表头节点。
- 保持 `fast` 指针在相遇点不动。
- 然后，让 `slow` 和 `fast` 同时以每次一步的速度前进。
- 当它们再次相遇时，这个相遇点就是链表的环入口节点